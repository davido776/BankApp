
using System;

namespace PeoplesBank
{
    class Program
    {
       
        static void Main(string[] args)
        {
            //Account newacct = new Account("0988",1000,"1234","savings");
            //var date = DateTime.Now;
            //string acctno = Helper.GenerateNumber();
            //var id = Guid.NewGuid().ToString();
            //var hashes = Helper.GenerateHash("1234");
            //byte[] passwordhash = hashes[0];
            //byte[] passwordsalt = hashes[1];
            //Customer newcust = new Customer(id, "david", "ologun", "d@g.com", date, passwordhash, passwordsalt);
            ////Account account = new Account(savingAccount);
            //GlobalConfig.AddIinstance();
            //AccountRepository acctrepo = GlobalConfig.Iinstance;
            //CustomerRepository custrepo = GlobalConfig.cIinstance;
            //custrepo.CreateCustomer(newcust);
            //var acctdata = GlobalConfig.dataIinstance;
            //acctdata.LoadModelsFromFile();
            //Console.WriteLine(AccountData.Accounts[0].Type);
            //acctrepo.CreateAccount(newacct);
            //Console.WriteLine(AccountData.Accounts[0].AccountNumber);
            
        }
    }
}
